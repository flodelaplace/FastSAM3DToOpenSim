pytorch-lightning
pyrender
opencv-python
yacs
scikit-image
einops
timm
rich
chumpy
roma
joblib
xtcocotools
loguru
optree
fvcore
pycocotools
huggingface_hub
smplx
scipy
tqdm
pyzmq
pygltflib==1.16.5
trimesh
# vtk : nécessaire pour lire les .vtp (Pose2Sim Geometry/) dans
# sam_3d_body/export/opensim_anatomical.py:_load_vtp. Sans ça, les meshes
# anatomiques ne se chargent pas et l'anatomical.glb sort vide.
vtk
braceexpand
termcolor
onnx
onnxscript
onnxruntime-gpu>=1.17.0
omegaconf
ultralytics
networkx==3.2.1
# Le worker persistant (worker/loop.py) consomme SQS et pousse sur S3/SNS depuis
# Python. L'AWS CLI presente dans l'image ne suffit pas : c'est un binaire, pas
# le SDK. run_job.sh, lui, n'a jamais eu besoin que du CLI.
boto3